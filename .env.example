# TaniClaw environment configuration
# Copy to .env and fill in your values

# ── Database ──────────────────────────────────────────────────────────────────
TANICLAW_DATABASE_URL=postgresql://taniclaw:taniclaw@localhost:5432/taniclaw

# ── Server ────────────────────────────────────────────────────────────────────
TANICLAW_HOST=0.0.0.0
TANICLAW_PORT=8000
TANICLAW_LOG_LEVEL=INFO

# ── Scheduler ─────────────────────────────────────────────────────────────────
TANICLAW_SCHEDULER_INTERVAL_MINUTES=60
TANICLAW_TIMEZONE=Asia/Jakarta

# ── Weather ───────────────────────────────────────────────────────────────────
# No API key needed — Open-Meteo is free
TANICLAW_WEATHER_API_BASE=https://api.open-meteo.com/v1

# ── LLM (Optional — Groq SDK) ─────────────────────────────────────────────────
TANICLAW_LLM_ENABLED=false
TANICLAW_GROQ_API_KEY=
TANICLAW_LLM_MODEL=llama-3.1-8b-instant
TANICLAW_LLM_FALLBACK_MODEL=llama-3.3-70b-versatile

# ── Security ──────────────────────────────────────────────────────────────────
TANICLAW_MAX_WATERING_AMOUNT_ML=500
TANICLAW_MAX_DAILY_ACTIONS=50
TANICLAW_MAX_FERTILIZER_GRAMS=20

# ── Notifications (Optional) ──────────────────────────────────────────────────
TANICLAW_NOTIFICATION_ENABLED=false
# TANICLAW_TELEGRAM_BOT_TOKEN=
# TANICLAW_TELEGRAM_CHAT_ID=
# TANICLAW_WHATSAPP_API_URL=
# TANICLAW_WHATSAPP_API_KEY=
